# docker-compose.yml

services:
  # Next.js Application
  app:
    build: .
    container_name: sate-jip-app-diskominfo
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    # depends_on:
    #   - db
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
# services:
#   # Next.js Application
#   app:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: sate-jip-app-diskominfo
#     restart: unless-stopped
#     ports:
#       - "3000:3000"
#     env_file:
#       - .env.local  # Use .env.local for production secrets
#     environment:
#       - NODE_ENV=production
#     # Remove volumes for production - app is self-contained in the image
#     # If using external database, uncomment:
#     # depends_on:
#     #   - db
#     networks:
#       - sate-jip-network

# networks:
#   sate-jip-network:
#     driver: bridge
